# 2026-09-05 · develop rebase 到 upstream v0.2.0 —— 决策记录

- 基线：`aa236488351eb71e120fc2b6fb32e36b0374c918`（`git ls-remote` 剥离 `v0.2.0^{}` 确认；tag 之后 upstream/main 另有 VERSION sync + sponsors + PR #6179，按约定不取）。
- 起点：v0.1.183（`e8cb019fa`）；上游区间 256 提交（含 v0.1.184 / v0.1.185），fork 重放 101 提交；备份分支 `backup/develop-pre-v0.2.0`。
- 停车 6 次：VERSION 链首 + 末（0.0.24 / 0.0.35，取 fork）、`UseKeyModal.vue`+spec（#6 恢复模板加载：import 与末尾追加测试）、`openai_gateway_grok.go`（#8）、`openai_gateway_chat_completions_raw.go`（#10）、GLM Claude Code 模板提交（`UseKeyModal.vue`/spec + en/zh `dashboard.ts`）。
- range-diff：87 `=`，8 `!`（4 处是手工解冲突，4 处仅上下文行/标识符重命名），VERSION 0.0.24 因基线不同被判成 `<`/`>` 同一提交。

## 收编 / 保留判定（谁覆盖面广用谁）

| 主题 | 判定 | 依据 |
|------|------|------|
| PR #6229 upstream endpoint observability vs fork 第 ① 层 `UpstreamURL` | **不重合，保留 fork** | 上游写的是 `OpsErrorLogEntry.UpstreamEndpoint`（归一化路径，handler 侧错误日志条目）；fork 填的是 `OpsUpstreamErrorEvent.UpstreamURL`（host+path，attempt 级 request_error 事件；上游对 HTTP 状态错误本就填此字段）。字段不同、结构不同，互补而非双写。 |
| #10 keepalive vs PR #6299 raw 流截断检测 | **合并，两者都要** | 上游 `openAIRawStreamTerminalState` + 截断 failover/错误分支并入 fork `finalize` 闭包；fork 独有一行：截断 failover 错误带 `SafeToFailoverAfterWrite = keepaliveWritten`。上游 10 个新截断用例走同步快路径零改动通过；fork 5 个保活用例不动。 |
| #8 grok max/ultra 顶档 | **保留 fork 语义差** | 上游仅把白名单函数导出为 `GrokSupportsXHighReasoningEffort`（Codex 目录声明复用），未加 max/ultra 用例；fork 合并 case 改调导出名。 |
| CN responses 探测 | **保留 fork** | 上游探测仍无 `normalizeDeepSeekResponsesRequestBody`、`ShouldUseResponsesAPI` 逃生口仍在。 |
| Claude Code 模板 `CLAUDE_CODE_ATTRIBUTION_HEADER=0` | **取上游（移除）** | 上游 `c03776604` 有意从全部模板与测试里移除；fork GLM 模板与 CCS 导入 env 同步跟随（否则 modal 与一键导入分叉）。 |
| GLM 计费 glm-5.3、Grok ZDR 文件降级 | **保留 fork，补登记** | 上游 v0.2.0 均未覆盖；此前未在 FORK.md 登记，本轮补入「未纳入 OpenSpec」表。 |

## 上游用例暴露的 fork 回归（rebase 时修）

- `UseKeyModal.vue` `currentFiles` 的 `case 'zhipu'` 未转发 `codex` tab，遮蔽了上游 v0.2.0 的 routed 目录 `default:` 分支 → 补 `generateRoutedCodexFiles(apiBase, apiKey, 'zhipu')`（`9a20a0036`）。
- `PUT /keys/:id/group` 契约夹具缺 `max_reasoning_effort_over_limit`（上游 group DTO 新字段，与 v0.1.176 的 `long_context_pricing_enabled` 同模式）（`21b15bff7`）。

## 验证

- `python3 tools/fork_overlay.py verify --base aa2364883`：11/11 OK；18 个 Shared Touchpoints 逐个 `git diff` 非零。
- 第 ① 层：`go build ./...`、`go vet -tags=unit ./...` 全绿，非测试 23 / 测试 15 个调用点全部带第 6 参；② 层三处 hunk 在位；③ 层 false/false/true。
- 后端：`go test ./...` 49 包 ok；`go test -tags=unit ./...` 修夹具后 ok。
- 前端：vitest 253 文件 / 1868 用例（修复后相关 15 文件 126 用例复跑绿）；`vue-tsc --noEmit` 干净。
- Go 工具链 1.27.0、golangci-lint v2.13、前端 lockfile 均无变化，release.yml 无需同步。

## 下轮决策点

- **PR #6179（`432ebc498`，upstream error proxy attribution，tag 后）**：helper 内加 `ProxyID`/`ProxyName`（fork 23 个调用点自动继承）；但它同时改了 ② 层三个文件里被 fork 整段替换掉的内联块 → 下轮必冲突或静默合成，解法是保留 fork 的 helper 调用；新增 `openai_upstream_transport_error_handle_test.go` 测试调用点只有 `go vet -tags=unit` 能抓。
- develop-lite 未动（欠 v0.1.138→v0.2.0）。
